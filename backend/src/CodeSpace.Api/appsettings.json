{
  "Serilog": {
    "Application": "CodeSpace.Api"
  },
  "_doc:HangfireHosting": "Which role this process plays in the Hangfire topology: 'Worker' runs the servers, owns recurring-job scheduling and executes agent runs; 'Api' registers storage + the job client only, so the pod enqueues and serves the dashboard but processes nothing. Both roles run the same assembly — a two-pod deployment sets Api on the public pod and Worker on the processing pod. Worker is the default so a single all-in-one process behaves as it always has.",
  "HangfireHosting": "Worker",
  "CodeSpaceStore": {
    "ConnectionString": "Host=localhost;Port=5432;Database=codespace;Username=codespace;Password=codespace"
  },
  "Webhooks": {
    "_doc": "The PUBLICLY REACHABLE base URL of this API. Provider webhook registration writes it into GitHub/GitLab, so a wrong value registers a hook the provider can never deliver to. Must be overridden in any deployment the internet can reach — the value here only makes the local loopback case explicit.",
    "BaseUrl": "https://localhost"
  },
  "Authentication": {
    "Jwt": {
      "SymmetricKey": "dev-only-do-not-use-in-prod-minimum-32-chars-please"
    }
  },
  "OAuth": {
    "CallbackUrl": "http://localhost:5180/api/credentials/oauth/callback"
  },
  "Cors": {
    "_doc": "Origins allowed to call this API cross-origin (when the SPA bypasses the Vite /api proxy). localhost:5180 is added automatically in Development (matches vite.config.ts server.port); production deployments must list their SPA host explicitly.",
    "AllowedOrigins": []
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
